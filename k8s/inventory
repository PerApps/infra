# use --ask-pass and --ask-become-pass in cli if you want to use a password instead of the private key.
# use export ANSIBLE_HOST_KEY_CHECKING=False for skip fingerprint in known_hosts file.
# <ip> ansible_connection=ssh ansible_user=<user> ansible_ssh_private_key_file=<ssh_key>

[k8s_master]

[k8s_worker]